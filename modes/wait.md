# Ralph — Wait Mode

PR #{{PR_NUMBER}} in `{{REPO}}` is open and waiting for a Copilot review that has not yet completed.

There is nothing to do this iteration. The outer loop will retry on the next iteration.

## Step 1 — Stop

Emit the following token as your **final output** and stop:

<promise>STOP</promise>

Any output after this token violates the rules.
